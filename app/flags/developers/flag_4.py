name = 'Add new blue ability'
description = 'Add a new ability - manually - to the Response plugin which prints out all network connections a host ' \
              'is making. Ensure this ability will run more than once if used in an operation, then ensure it is ' \
              'loaded into the database.'


async def verify(services):
    return False
