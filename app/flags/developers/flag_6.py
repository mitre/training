name = 'Encoded payloads'
description = 'Using the same payload as the previous step, encode the payload and ensure that downloading it ' \
              'through the REST API works as expected'


async def verify(services):
    return False
