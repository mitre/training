name = 'Write new parser'
description = 'Add a new parser which parses IPV6 addresses with the trait, "host.ip.ipv6". ' \
              'Then run an adversary which correctly parses out matching facts.'


async def verify(services):
    return False
