name = 'Build a new agent'
description = 'Write a new agent in either bash or PowerShell, using the HTTP contact. Then run a new operation ' \
              'against the agent, selecting no profile, and sending at least 5 potential links to it.'


async def verify(services):
    return False
